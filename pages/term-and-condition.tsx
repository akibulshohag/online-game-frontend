import request from '../lib/request';

const TermsAndConditions = (props:any) => {
    const {page} = props;
    const termsAndCondition = page.termsCondition;
    return (
      <>
        {/* <Head>
          <title>Terms And Condition</title>
          <meta name="Description" content="Generated by create your app"></meta>
          <link rel='icon' href="/Logo.png"></link>
        </Head> */}
        <div style={{marginTop: '60px', minHeight: '600px',padding:'20px'}} dangerouslySetInnerHTML={{__html: termsAndCondition}}>
   
        </div>
        </>
    );
};

export default TermsAndConditions;
 

export async function getServerSideProps(context:any) {
  
    const page = await request(`terms-condition`,'');
  
  
  
    return {
      props: {
        page: page?.data[0],
        // fallback: false,
      }
    }
  }