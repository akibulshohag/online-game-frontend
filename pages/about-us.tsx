import request from '../lib/request';

const AboutUs = (props:any) => {
    const {page} = props;
    const aboutUs = page.aboutUs;
    return (
      <>
        {/* <Head>
          <title>Terms And Condition</title>
          <meta name="Description" content="Generated by create your app"></meta>
          <link rel='icon' href="/Logo.png"></link>
        </Head> */}
        <div style={{marginTop: '60px', minHeight: '600px',padding:'20px'}} dangerouslySetInnerHTML={{__html: aboutUs}}>
   
        </div>
        </>
    );
};

export default AboutUs;
 

export async function getServerSideProps(context:any) {
  
    const page = await request(`about-us`,'');
  
  
  
    return {
      props: {
        page: page?.data[0],
        // fallback: false,
      }
    }
  }