import request from '../lib/request';

const PrivacyPolicy = (props:any) => {
    const {page} = props;
    const privacyPolicy = page.privacyPolicy;
    return (
      <>
        {/* <Head>
          <title>Terms And Condition</title>
          <meta name="Description" content="Generated by create your app"></meta>
          <link rel='icon' href="/Logo.png"></link>
        </Head> */}
        <div style={{marginTop: '60px', minHeight: '600px',padding:'20px'}} dangerouslySetInnerHTML={{__html: privacyPolicy}}>
   
        </div>
        </>
    );
};

export default PrivacyPolicy;
 

export async function getServerSideProps(context:any) {
  
    const page = await request(`privacy-policy`,'');
  
  
  
    return {
      props: {
        page: page?.data[0],
        // fallback: false,
      }
    }
  }